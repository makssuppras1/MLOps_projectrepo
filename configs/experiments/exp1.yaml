# @package _global_
# Experiment 1: Default configuration
# This file can override any parameters from model_conf or training_conf
training:
  seed: 42
  batch_size: 32
  epochs: 10
  optimizer:
    lr: 0.001
